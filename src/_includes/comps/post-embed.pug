include post-info 

mixin post-embed(post)

	article.card.mb-3
		.card-content
			- const href = post.dest.path.match(/(.+)\/index/)[1]
			
			.media
				.media-content
					h2.title.is-4
						a(href=href)= post.data.title
					+post-info(post)
			.content
				!=filters.md(post.data.description)
