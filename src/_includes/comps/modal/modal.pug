mixin modal(id)
	.toggleable-modal
		input.modal-toggle(type="checkbox" id=id)
		.modal
			label.modal-background(for=id)

			.modal-content
				block
				label.modal-close.is-large(for=id aria-label="close")