{{ layout "layouts/base.vto" }}

<main class="py-20 max-w-screen-lg mx-auto sm:py-10">
	{{ if it.title && !it.doNotRenderTitle }}
		<h1 class="text-4xl font-bold mt-20">{{ title }}</h1>
	{{ /if }}

	{{ content }}
</main>

{{ /layout }}

